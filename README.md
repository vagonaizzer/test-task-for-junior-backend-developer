# Task Service

Сервис для управления задачами с HTTP API на Go.


## Что было добавлено: периодичность задач

### Суть

Задачи теперь могут быть периодическими — врач один раз создаёт задачу и указывает, по какому расписанию она должна выполняться. Чтобы посмотреть, какие задачи актуальны на конкретный день, есть отдельный эндпоинт.

### Типы периодичности

| Тип | `"type"` | Что ещё нужно указать |
|---|---|---|
| Каждые N дней | `"daily"` | `"interval"` — раз в сколько дней (≥ 1) |
| Конкретное число месяца | `"monthly"` | `"day_of_month"` — число от 1 до 30 |
| Конкретные даты | `"specific_dates"` | `"dates"` — массив дат в формате `YYYY-MM-DD` |
| Чётные / нечётные дни | `"even_odd"` | `"even_odd"` — `"even"` или `"odd"` |

### Новый эндпоинт

```
GET /api/v1/tasks/scheduled?date=2026-04-10
```

Возвращает все периодические задачи, которые попадают на указанную дату. Если параметр `date` не передать — вернёт задачи на сегодня.

### Примеры

Создать задачу с ежедневным обзвоном (каждые 2 дня):

```json
POST /api/v1/tasks
{
  "title": "Обзвон пациентов",
  "recurrence": {
    "type": "daily",
    "interval": 2
  }
}
```

Задача на каждое 15-е число месяца:

```json
{
  "title": "Формирование отчётности",
  "recurrence": {
    "type": "monthly",
    "day_of_month": 15
  }
}
```

Только по чётным числам:

```json
{
  "title": "Инвентаризация",
  "recurrence": {
    "type": "even_odd",
    "even_odd": "even"
  }
}
```

### Допущения

- **`monthly` ограничен 30-м числом** — чтобы не было ситуации когда задача с `day_of_month: 31` никогда не срабатывает в феврале, апреле и т.д. Если нужна логика "последнее число месяца" — это отдельная фича.
- **`daily` считает дни от даты создания задачи** — то есть если задача создана 1 апреля с `interval: 3`, то следующие даты: 1, 4, 7 апреля и т.д. День создания тоже считается подходящим.
- **Настройки периодичности хранятся как JSONB** — гибкий формат, не нужно менять схему при добавлении нового типа периодичности.
- **`/tasks/scheduled` возвращает только периодические задачи** — обычные одноразовые задачи туда не попадают, у них нет поля `recurrence`.
- **При обновлении задачи** (`PUT /tasks/{id}`) можно убрать периодичность, просто не передав поле `recurrence`. Передал — установилась, не передал — сбросилась.

### Миграция

Перед запуском нужно применить новую миграцию:

```sql
-- migrations/0002_add_task_recurrence.up.sql
ALTER TABLE tasks ADD COLUMN IF NOT EXISTS recurrence JSONB;
```

Если запускаешь через Docker Compose с нуля — достаточно добавить файл миграции в маунт в `docker-compose.yml` (по аналогии с первой миграцией). Если база уже существует — выполни SQL вручную или через любой инструмент миграций.

## Требования

- Go `1.23+`
- Docker и Docker Compose

## Быстрый запуск через Docker Compose

```bash
docker compose up --build
```

После запуска сервис будет доступен по адресу `http://localhost:8080`.

Если `postgres` уже запускался ранее со старой схемой, пересоздай volume:

```bash
docker compose down -v
docker compose up --build
```

Причина в том, что SQL-файл из `migrations/0001_create_tasks.up.sql` монтируется в `docker-entrypoint-initdb.d` и применяется только при инициализации пустого data volume.

## Swagger

Swagger UI:

```text
http://localhost:8080/swagger/
```

OpenAPI JSON:

```text
http://localhost:8080/swagger/openapi.json
```

## API

Базовый префикс API:

```text
/api/v1
```

Основные маршруты:

- `POST /api/v1/tasks`
- `GET /api/v1/tasks`
- `GET /api/v1/tasks/{id}`
- `PUT /api/v1/tasks/{id}`
- `DELETE /api/v1/tasks/{id}`
